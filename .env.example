# Required for Vercel Blob Storage (large file uploads)
# 1. Go to your Vercel project → Storage → Create a Blob Store
# 2. The token is auto-added to your project env vars
# 3. For local dev: run `vercel env pull .env.local`
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...
